# dsrag_minimal/requirements.txt

# Core dsRAG Minimal
numpy>=1.21.0
pydantic>=2.5.0
instructor>=1.3.0 # Verifica versión compatible con Pydantic 2
tqdm>=4.60.0
langchain-text-splitters>=0.3.0

# Vector DB (Exclusivo)
qdrant-client>=1.9.0

# LLM / Embedding Local (Ollama)
ollama>=0.2.0

# LLM / Embedding API (Opcional, para ejemplos/flexibilidad)
openai>=1.10.0
anthropic>=0.20.0
# google-generativeai>=0.3.0 # Si se añade soporte Gemini

# File Parsing (No VLM)
pypdf2>=3.0.0
python-docx>=1.1.0 # Nombre pip para docx2txt dependency si es el que se usa, o docx2txt directamente si se prefiere
# docx2txt>=0.8 # Alternativa

# Dependencias Transitivas (Comunes, pueden necesitar ajuste)
# requests>=2.25.0
# typing-extensions>=4.0.0 # A menudo requerida por Pydantic/Instructor
# httpx>=0.25.0 # Usado por openai/anthropic
# PyYAML>=6.0 # Usado por langchain-core (dependencia de langchain-text-splitters)
# jsonpatch / jsonpointer # Usado por langchain-core